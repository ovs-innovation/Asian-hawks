"use client";

import SimpleWorkspace from "@/components/simple-workspace";

export default function CmsPage() {
  return (
    <SimpleWorkspace
      title="CMS"
      body="CMS pages can be expanded here. Job postings and categories are already live under Jobs."
    />
  );
}
