"use client";

import SimpleWorkspace from "@/components/simple-workspace";

export default function BlogsPage() {
  return (
    <SimpleWorkspace
      title="Blogs"
      body="Blog management is available through existing platform APIs. Use this space to review hiring content later."
    />
  );
}
