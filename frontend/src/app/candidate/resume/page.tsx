"use client";

import { WorkspacePage } from "@/components/layout/workspace-page";

export default function Page() {
  return <WorkspacePage title="Resume builder" body="Write once. Attach it to every application." cta="Open builder" href="/resume-builder" />;
}
