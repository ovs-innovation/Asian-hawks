import { ComingSoon } from "@/components/platform/coming-soon";

export default function ResumeBuilderPage() {
  return (
    <ComingSoon
      title="Resume Builder"
      body="AI resume builder, review, and mock interviews land in Phase 3. You can already upload a resume from your candidate profile."
    />
  );
}
