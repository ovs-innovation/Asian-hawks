"use client";

import { WorkspacePage } from "@/components/layout/workspace-page";

export default function Page() {
  return <WorkspacePage title="Expired jobs" body="Roles past their application deadline." />;
}
