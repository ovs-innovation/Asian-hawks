export default function InstituteStudentsPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-[24px] font-semibold">Students</h1>
      <div className="rounded-[var(--radius)] border border-[var(--border)] bg-[var(--card)] p-8 text-center text-[var(--text-secondary)]">
        Student roster and enrollment management — connect backend to populate.
      </div>
    </div>
  );
}
